<div class="card">
    <div class="card-header">
        <h4><a class="link" href="/viewpost/{{id}}">{{title}}</a></h4>
        <p>Posted by {{user.username}} on {{format_date date_created}}</p>
        <p class="card-body">{{content}}</p>
    </div>
</div>